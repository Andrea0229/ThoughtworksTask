var _ = require("lodash");
var chai = require("chai");
var sinon = require("sinon");
var sinonChai = require("sinon-chai");
var expect = chai.expect;
chai.use(sinonChai);

describe("binary exercise ", function () {
    it("test: 数字转换为指定二进制", function () {
        let expect_binary = "0011010";
        let result;

        expect(expect_binary).to.equal(result);

    })
})
